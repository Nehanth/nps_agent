# =============================================================================
# NPS Agent Dependencies
# Single requirements file for all stages: develop, deploy, observe
# =============================================================================

# MCP (Model Context Protocol) stack
fastmcp
mcp
starlette
httpx

# Core utilities
python-dotenv
litellm
tenacity

# OpenAI Agents SDK (pinned for stability)
openai-agents==0.9.0

# MLflow - ODH fork with RHOAI-specific features
mlflow @ git+https://github.com/opendatahub-io/mlflow@master

# Async event loop support for production serving
nest_asyncio

# Fast package installer for container builds
uv

# Notebook dependencies (for local development)
ipykernel
ipywidgets
