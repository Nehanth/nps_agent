# Observe

Walks through the agent observation process with MLflow.

Shows how to capture traces from a running agent into a dataset, how to run an outer-loop evaluation, and how to view the results.

- The viewer learns how to navigate the MLflow UI, find the traces for an experiment, and view the results of the AaaJ for their deployed agent using the MLflow in RHOAI.